#This property determines whether a call to HystrixCommand.getFallback() will be attempted when failure or rejection occurs. It's default value is true hence marking it as false. We will do practice with fallback in next coding exercise
hystrix.command.default.fallback.enabled=true